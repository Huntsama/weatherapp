# weatherapp

A weather app built with React. Fetches current conditions for a searched city from a
public forecast API and renders them in a single-page view.

## Stack

React, axios, Create React App.

## Running it

```bash
npm install
npm start
```

Runs on http://localhost:3000.
